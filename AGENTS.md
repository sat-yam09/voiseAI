# AGENTS.md

## Project state
- Greenfield repository (currently empty). Directory name suggests a voice RAG project, but no code, manifests, or tooling exist yet.
- There is no README, package manager config, CI, or existing instructions to follow.

## Do not touch
- `.snapshots/` is generated by the "Snapshots for AI" VS Code extension. Treat it as a build artifact; never edit its contents.
- `.snapshots/config.json` controls which files the extension includes in snapshots. Change it only if the user asks.

## Working here
- Before writing code, confirm the intended stack and layout with the user; nothing about the project structure is established.
- No lint, test, or build commands exist yet. Do not invent them.
