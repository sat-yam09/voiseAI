# Set this to the FastAPI bridge when running the real retrieval pipeline.
RETRIEVAL_API_URL=http://127.0.0.1:8000
# Set false in production so an unavailable upstream is surfaced instead of mocked.
ALLOW_DEMO_FALLBACK=true
